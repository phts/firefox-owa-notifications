{
  "name": "owa-notifications",
  "private": true,
  "scripts": {
    "build": "npm-run-all -s lint clean xpi",
    "clean": "rimraf dist",
    "lint": "eslint .",
    "xpi": "node scripts/build.js"
  },
  "devDependencies": {
    "@phts/eslint-config": "^1.0.1",
    "adm-zip": "^0.4.11",
    "eslint": "^5.6.0",
    "npm-run-all": "^4.1.3",
    "rimraf": "^2.6.2"
  }
}
