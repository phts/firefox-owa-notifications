{
  "name": "owa-notifications",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rimraf dist",
    "prebuild": "npm run clean"
  },
  "devDependencies": {
    "adm-zip": "^0.4.11",
    "rimraf": "^2.6.2"
  }
}
